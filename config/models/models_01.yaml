# to get the descriptions of tables using their images and context
table_descriptions:
  _target_: src.utils.utils.get_llm_provider
  name: gemma-3-27b-it
  temp: 1.0

# to get the summary of each header section in the markdown file generated by minerU
section_summaries:
  _target_: src.utils.utils.get_llm_provider
  name: gemma-3-27b-it
  temp: 1.0

# to split large chunks into smaller sentences where this was not done by the splitters
chunking:
  _target_: src.utils.utils.get_llm_provider
  name: mistral-large-latest
  temp: 0.2

# to de-contextualize individual sentences given their context
decontextualization:
  _target_: src.utils.utils.get_llm_provider
  name: mistral-large-latest
  temp: 0.2
